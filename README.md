Clone the repository to C drive's xampp>htdocs.
There should be a few files ending with the extension .sql, make a database called blood_bank_management_system in xampp, then import the latest .sql file onto there.
After starting xampp as intended, if repository is placed correctly then the link to acces it would be: http://localhost/Blood%20Bank%20Management%20System/
To access any files or pages, just add the file name as is at the end of the link.
